{
  "name": "fixup-twitter-link",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "start": "node .",
    "dev": "node -r dotenv/config . dotenv_config_override=true",
    "build": "swc src --only **/*.ts --out-dir dist && node -e '!fs.existsSync(\".sentryclirc\") || process.exit(1)' || pnpm sentry:sourcemaps",
    "build:debug": "swc --source-maps=true src --out-dir=dist && node -e '!fs.existsSync(\".sentryclirc\") || process.exit(1)' || pnpm sentry:sourcemaps",
    "type:check": "tsc --noEmit",
    "lint": "eslint --cache ./src",
    "lint:fix": "eslint --fix --cache ./src",
    "format": "dprint fmt",
    "format:check": "dprint check",
    "test": "jest",
    "sentry:sourcemaps": "sentry-cli sourcemaps inject --org mmmm-q0 --project fixup-twitter-link ./dist && sentry-cli sourcemaps upload --org mmmm-q0 --project fixup-twitter-link ./dist"
  },
  "license": "MIT",
  "packageManager": "pnpm@8.10.2",
  "engines": {
    "npm": "This project is configured to use pnpm"
  },
  "devDependencies": {
    "@sentry/cli": "2.21.3",
    "@swc/cli": "0.1.62",
    "@swc/core": "1.3.96",
    "@swc/jest": "0.2.29",
    "@tsconfig/node20": "20.1.2",
    "@tsconfig/strictest": "2.0.2",
    "@types/jest": "29.5.8",
    "@types/node": "20.9.0",
    "@typescript-eslint/eslint-plugin": "6.10.0",
    "@typescript-eslint/parser": "6.10.0",
    "dotenv": "16.3.1",
    "dprint": "0.42.5",
    "eslint": "8.53.0",
    "eslint-config-prettier": "9.0.0",
    "jest": "29.7.0",
    "typescript": "5.2.2"
  },
  "dependencies": {
    "@discordjs/collection": "1.5.3",
    "@discordjs/core": "1.0.1",
    "@discordjs/rest": "2.0.1",
    "@discordjs/ws": "1.0.1",
    "@sentry/node": "7.80.0",
    "@sentry/profiling-node": "1.2.6"
  },
  "private": true
}
